
let b = `valor`;
//usando o sibolo ${} para fazer interpolação de variáveis, fica muito mais simples,
// pode ser qualquer tipo de variável
var a = `
<p>usando a crase pra usar strings multilinhas sem precisar usa + para concatenar as strings</p>
<p>usando a crase pra usar strings multilinhas sem precisar usa + para concatenar as strings</p>
${b}
<p>usando a crase pra usar strings multilinhas sem precisar usa + para concatenar as strings</p>
${000010101001}
<p>usando a crase pra usar strings multilinhas sem precisar usa + para concatenar as strings</p>
${b}
<p>usando a crase pra usar strings multilinhas sem precisar usa + para concatenar as strings</p>

 ` ;